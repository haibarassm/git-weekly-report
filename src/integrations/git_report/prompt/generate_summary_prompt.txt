请根据以下 commit 标题和任务列表生成一个简洁的摘要。

{input_info}

要求：
1. 摘要必须能让人看懂做了什么，不能只有一两个词
2. 使用中文描述，保留必要的英文技术术语
3. **必须使用中文！禁止输出日语、韩语等其他语言**
4. **优先使用技术关键词**：
   - 如果任务中包含技术名词（如 parent_code、权限、国际化等），必须使用
5. 业务化表达：使用功能名词而非动作描述
6. 禁止使用泛词（禁止：改动、调整、处理、修改、完善）
7. 对于发布类 commits，必须移除所有环境词（新加坡/德国/巴西/SG/DE/BR等）
8. 如果检测到状态（已发布/已提测/对接中），请在摘要末尾加上(状态)
9. 只返回摘要文本本身，不要加引号、书名号等包裹符号
10. 末尾不要加句号
11. 摘要控制在20字以内

**返回示例**（直接输出文本，不加引号）：
Card支付收银台埋点功能(对接中)
修复卡号掩码长度计算错误(已发布)
CVV输入安全漏洞修复(已发布)
菜单国际化支持(已提测)
添加CVV长度字段和卡号脱敏功能(对接中)

**错误示例**（不要这样返回）：
"修复卡号掩码长度计算错误(已发布)"  ← 不要加引号
Card支払いのレジ機能です  ← 禁止输出日语
添加了功能并且完成了开发和测试以及发布到新加坡环境(已发布)  ← 太长太冗余

状态判定规则（必须严格执行）：
- 同时包含"发布"/"上线"**和**地区关键词（如"新加坡"、"德国"、"巴西"等）→ "已发布"
- 只有"发布"/"上线"但没有地区关键词 → "已提测"
- 包含"提测"、"测试"等关键词 → "已提测"
- 包含"开发"、"实现"、"添加"等开发类词汇 → "对接中"

重要：不要包含任何环境名称！

摘要：