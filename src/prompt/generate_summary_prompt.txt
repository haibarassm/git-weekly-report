请根据以下 commit 标题和任务列表生成一个简洁的摘要（最多15个字）。

{input_info}

要求：
1. 摘要应反映这些任务的共同主题或核心功能
2. 使用简洁的中文描述
3. **优先使用技术关键词**：
   - 如果任务中包含技术名词（如 parent_code、权限、国际化等），必须使用
   - 例如：包含 parent_code 的任务 → "parent_code处理" 或 "一级菜单parent_code"
   - 例如：包含权限的任务 → "权限相关" 或 "权限管理"
   - 例如：包含国际化的任务 → "国际化" 或 "菜单国际化"
4. 业务化表达：使用功能名词而非动作描述
   - 例如："子用户改动" → "子用户功能"
   - 例如："菜单国际化修复" → "菜单国际化"
5. 禁止使用泛词（除非绝对必要）：
   - 禁止：改动、调整、处理、修改、完善
   - 可以：功能、支持、能力、优化（当是优化类工作时）
6. 对于发布类 commits，必须移除所有环境词（新加坡/德国/巴西/SG/DE/BR等）
7. 如果检测到状态（已发布/已提测/对接中），请在摘要末尾加上(状态)
8. 只返回摘要，不要其他内容

状态判定规则（必须严格执行）：
- 同时包含"发布"/"上线"**和**地区关键词（如"新加坡"、"德国"、"巴西"等）→ "已发布"
- 只有"发布"/"上线"但没有地区关键词 → "已提测"
- 包含"提测"、"测试"等关键词 → "已提测"
- 包含"开发"、"实现"、"添加"等开发类词汇 → "对接中"

重要：不要包含任何环境名称！

摘要：
