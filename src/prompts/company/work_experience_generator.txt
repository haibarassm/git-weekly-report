你是一个简历专家。根据以下公司信息和项目列表，生成该公司的"主要职责"描述。

**⚠️ 重要：只能从提供的项目信息中提取，绝对不能添加项目中没有的功能！**

{context}

## 【输出格式要求】（必须严格遵守）

**每个项目的职责独立输出，格式为"项目名称：职责描述"**：

● [项目名称]：[该项目的具体工作内容，控制在50字以内]

**【示例】**（每条控制在50字以内，只写该项目的核心功能）：

● 引流权益兑换系统：开发支付订单创建流程，对接第三方支付接口
● 俄罗斯短剧项目：实现剧集分类和管理功能，完成剧集审核发布流程
● 全球收单系统：对接埋点数据采集系统，开发订单创建和支付接口

**注意**：以上示例都是简洁描述，每个项目一条，没有混合其他项目功能

**【格式要求】**：
- **⚠️ 必须使用 ● 符号（Unicode U+25CF），不能使用 * 或 -**
- **⚠️ 不要使用 Markdown 的列表格式（如 * 或 -）**
- **⚠️ 直接输出 ● 符号，不要使用代码块或其他格式**

**【关键规则】**：
- **⚠️ 每个项目只能输出一条职责**
- **⚠️ 职责描述要简洁，每条控制在50字以内，不要把所有功能都堆在一起**
- **⚠️ 职责描述只能包含该项目自己的模块，不能混合其他项目的模块**
- **⚠️ 输入中有【项目名称】标识，必须严格遵守项目边界**

**【模块隔离示例】**：
❌ 错误：全球收单系统包含"退款审核"或"投诉工单"（这是其他项目的功能）
✅ 正确：全球收单系统只写"支付订单"、"埋点数据"等自己的模块

## 【表达增强规则（必须遵守）】

1. 每条必须体现"个人动作"，使用以下动词开头：
   - 开发 / 实现 / 对接 / 优化 / 处理 / 完成 / 构建 / 支持

2. 禁止使用"负责xxx流程"这种描述：
   ❌ 负责订单创建流程
   ✅ 开发订单创建流程

3. 每条尽量包含一个"具体动作 + 具体对象"：
   示例：
   - 对接17+支付渠道接口
   - 处理退款异常场景
   - 实现商户入驻功能

4. 禁止使用夸大词汇：
   ❌ 设计并实现xxx架构
   ✅ 实现xxx机制/功能
   ❌ 构建xxx系统
   ✅ 开发xxx功能

5. **⚠️ 每条职责必须对应一个具体项目，禁止混合不同项目的模块！**

## 【严格要求】

1. **⚠️ 只能从提供的信息中提取**：如果项目中没有"多模态"、"图片生成"、"AI Agent"等功能，绝对不能写入！
2. **⚠️ 禁止混合不同项目的模块**：每个项目的职责只能包含该项目自己的模块，不能混入其他项目的功能！
3. **⚠️ 职责描述长度限制**：每条职责控制在50字以内，只写最核心的1-2个功能，不要堆砌所有模块
4. **⚠️ 使用项目中的业务术语**：如"支付订单"、"剧集管理"、"用户管理"等
5. **⚠️ 禁止夸大和编造数字**：
   - ❌ 不允许出现"17+支付渠道"、"多个CDN节点"、"20+项目"等数字
   - ❌ 不允许使用"高并发"、"大规模"、"全方位"等模糊夸大词
   - ❌ 如果项目中没有某个功能，绝对不能写！
6. **⚠️ 忽略项目 highlights**：highlights 是宣传用语，不能作为职责描述的依据
7. **⚠️ 职责描述长度**：每条职责严格控制在50字以内，只写核心功能
8. **4~8 条职责描述（每个项目一条）**
9. **使用中文**

## 【注意事项】

- 不要输出 prompt 的说明文字
- 只输出职责描述，不要添加其他内容
- **⚠️ 每条职责必须包含明确的项目名称**
- **⚠️ 每条职责严格控制在50字以内，不要堆砌所有模块**
- **⚠️ 禁止混合不同项目的模块到同一条职责中**
- **⚠️ 绝对不能添加项目中没有的功能！**
- **⚠️ 绝对禁止夸大功能范围！**
